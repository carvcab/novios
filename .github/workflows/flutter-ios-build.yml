name: Flutter iOS Build

on:
  workflow_dispatch:
    inputs:
      build_id:
        description: 'Identificador único del build'
        required: true
        type: string

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Configure Flutter
        run: |
          flutter config --no-enable-swift-package-manager

      - name: Install dependencies
        run: |
          cd "iphone_flutter"
          flutter pub get

      - name: Build iOS App (No Signing)
        run: |
          cd "iphone_flutter"
          flutter build ios --no-codesign --debug

      - name: Package IPA
        run: |
          cd "iphone_flutter/build/ios/iphoneos" || cd "iphone_flutter/build/ios/Debug-iphoneos" || cd "iphone_flutter/build/ios/Release-iphoneos"
          mkdir -p Payload
          cp -r Runner.app Payload/
          zip -r "${{ inputs.build_id }}.ipa" Payload
          mkdir -p "${GITHUB_WORKSPACE}/iphone_flutter/ipa"
          cp "${{ inputs.build_id }}.ipa" "${GITHUB_WORKSPACE}/iphone_flutter/ipa/"

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ipa-flutter
          path: iphone_flutter/ipa/*.ipa
          retention-days: 30
